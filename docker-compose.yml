services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./Backend:/app
    restart: always

  frontend:
    build:
      context: ./UI
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./UI:/app
    restart: always
